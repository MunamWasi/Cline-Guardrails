# Copy to .env and fill values. Never commit real keys.

# Mode selector:
# - oss = local Citadel sidecar
# - pro = Mighty Gateway (API key)
MIGHTY_MODE=oss

# Pro mode key (leave empty for OSS)
MIGHTY_API_KEY=

# Hook decision thresholds
MIGHTY_WARN_THRESHOLD=0.70
MIGHTY_BLOCK_THRESHOLD=0.85

# Optional Gateway preference in pro mode
MIGHTY_PREFER_GATEWAY=1
MIGHTY_GATEWAY_URL=https://gateway.trymighty.ai

# Optional local Citadel settings
CITADEL_PORT=8787
CITADEL_MODE=input
CITADEL_TIMEOUT_SECONDS=2

# Optional debug from inner scanner layer (runner still keeps stdout clean)
# CITADEL_DEBUG=1
